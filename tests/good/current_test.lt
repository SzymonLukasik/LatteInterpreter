void main() {
    {}

    print("LOL");

    int a;
    print(a);

    int b = 10, c = 1;
    print(b);
    print(c);

    boolean t = true, f = false;
    boolean res = !true && (t || f);
    print(res);

    int res = b * c / 5 + c - 1000 / 10;
    print(res);

    
    boolean res = b < c;
    boolean res = b <= c;
    boolean res = b > c;
    boolean res = b >= c;
    boolean res = b == c;
    boolean res = b != c;
    print(res);

    int i = 0;
    while (i < 10) {
        print(i);
        i = i + 1;
    }

    
    if (true)
        print("TRUE");
    if (false)
        print("FALSE");
    else
        print("TRUE AGAIN");
    
    
}